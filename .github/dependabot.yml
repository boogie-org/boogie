version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
    # One grouped pull request per week rather than one per action.
    groups:
      actions:
        patterns:
          - "*"
