procedure {:checksum "0"} M();

implementation {:id "M"} {:checksum "2"} M()
{
    var x: int;
    var y: int;

    while (*)
    {
        x := 0;
        y := 0;
    }

    assert 0 == 0;
    assert x != x;
}
