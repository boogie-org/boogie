config.name = 'GPUVerify'
config.suffixes = ['.gbpl']
config.test_format = lit.formats.ShTest()

dotnet = ''
if sys.platform not in ['win32', 'cygwin']:
  dotnet = 'mono '

boogie_root = os.getcwd() + '/../..'

config.substitutions.append(('GPUVerify', dotnet + boogie_root + '/Source/GPUVerify/bin/' + lit.params['configuration'] + '/GPUVerify.exe'))
config.substitutions.append(('Boogie', dotnet + boogie_root + '/Binaries/Boogie.exe -typeEncoding:m -doModSetAnalysis'))

config.target_triple = 'foo'
